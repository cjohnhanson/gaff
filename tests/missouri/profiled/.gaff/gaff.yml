reminders:
  - name: keep
    every:
      tool_calls: 2
    text: kept under focus
  - name: drop
    every:
      tool_calls: 2
    text: dropped under focus
profiles:
  focus:
    disable:
      - drop
  quiet:
    disable:
      - keep
      - drop
transitions:
  agent_may_set:
    - focus
