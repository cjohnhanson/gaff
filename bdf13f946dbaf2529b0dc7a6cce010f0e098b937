signoff[fresh-eyes] PASS bdf13f946dbaf2529b0dc7a6cce010f0e098b937 independent agent built the branch green and origin/main red with the eight named git-driving failures, ran the suite against a PATH holding only gitMinimal, and verified every comment claim against the repo and nixpkgs; the fixture-isolation defect was found by running this gate and reproduced directly with GIT_DIR set
signoff[mutation] PASS bdf13f946dbaf2529b0dc7a6cce010f0e098b937 substituting checkInputs for nativeCheckInputs reproduces the identical eight failures, and dropping GIT_DIR from the fixture removal list turns the new isolation test red on its own
